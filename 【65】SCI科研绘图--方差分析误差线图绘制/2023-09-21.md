
### 欢迎关注R语言数据分析指南

>本节来复现**nature communications**中的一张论文图，**进行单向方差分析并做数据可视化**。由于作者为提供原始数据，因此结果会有所不同。下面小编通过一个案例来进行展示，图形仅供展示用过程仅参考，希望各位观众老爷能够喜欢。数据代码已经整合上传到2023VIP交流群，加群的观众老爷可自行下载，有需要的朋友可关注文末介绍加入VIP交流群

### 关注下方公众号下回更新不迷路

1

### 论文


![](https://files.mdnice.com/user/7338/e949503c-b976-484d-bbb6-4ccaa9a5b163.png)


### 论文原图

![](https://files.mdnice.com/user/7338/a0221630-74a7-4fc6-85b5-ccbd0fa85ab5.png)

### 加载R包
```r
library(tidyverse)
library(ggtext)
library(ggprism)
library(ggsignif)
library(rstatix)
library(ggpubr)
```
### 导入数据
```r
df <- read_tsv("data.txt") %>% pivot_longer(-Type) %>% 
  dplyr::filter(Type=="Day 5") %>% select(-Type)
```
### 方差分析
```r
# 使用aov函数对数据df进行方差分析，其中因变量是value，自变量是name
result.aov <- aov(value ~ name, data = df)

# 对方差分析的结果进行Tukey的HSD测试
result.tukey <- TukeyHSD(result.aov)

# 转换p值
aov_pvalue <- result.tukey$name %>% 
  # 将结果转换为数据框
  as.data.frame() %>% 
  # 将行名转换为一个新的列，命名为"group"
  rownames_to_column(var="group") %>% 
  # 仅选择"group"和"p adj"两列
  dplyr::select(1,`p adj`) %>% 
  # 将"group"列分割为"group2"和"group1"两列，分隔符为"CAR T-"
  separate(`group`, into=c("group2", "group1"), sep="CAR T-", convert = TRUE) %>% 
  # 将"group1"列中的"CAR T"替换为空字符串
  mutate(group1=str_replace_all(`group1`, "CAR T","")) %>% 
  # 重新选择列的顺序
  select(2,1,3) %>% 
  # 删除第一和第二列
  select(-1,-2) %>% 
  # 根据p值的大小，将其转换为相应的符号（**表示p<0.01，*表示p<0.05，ns表示p>=0.05）
  mutate(p_signif=symnum(`p adj`, corr = FALSE, na = FALSE,  
                         cutpoints = c(0, 0.01, 0.05,1), 
                         symbols = c("**", "*", "ns")))

```
### 构建显著性标记文件
>在此先使用rstatix包内置的wilcox_test函数来进行统计分析得到显著性的位置信息，之后将方差分析的结果合并这样方便后期使用函数自动添加显著性标记。
```r
df_pvalue <- df %>% 
  # 将"name"列中的"CAR T"替换为空字符串
  mutate(`name`=str_replace_all(`name`, "CAR T","")) %>% 
  mutate(name=str_trim(name)) %>%  #去除两侧的空格
  wilcox_test(value ~ name) %>% 
  add_significance(p.col="p.adj") %>%  # 添加显著性标记，基于调整后的p值
  add_xy_position(x="name") %>%    # 添加x和y坐标的位置，基于"name"列
  select(-p.adj) %>% 
  # 将处理后的数据框与aov_pvalue数据框进行列合并
  bind_cols(aov_pvalue)
```
### 数据可视化
```r
df %>%
  # 将"name"列中的"CAR T"替换为空字符串
  mutate(`name`=str_replace(`name`, "CAR T","")) %>% 
  ggplot(aes(name,value))+
  # 添加均值的误差线
  stat_summary(fun = mean,geom = "errorbar", width=.2,
               fun.max = function(x) mean(x) + sd(x),
               fun.min = function(x) mean(x) - sd(x)) +
  # 添加均值的横线
  stat_summary(fun = mean, geom = "crossbar",width = 0.4,color = "black",size=0.5) +
  # 添加散点
  geom_jitter(aes(fill=name,color=name,shape=name),width = 0.1, height = 0)+
  # 手动添加p值标签
  stat_pvalue_manual(df_pvalue,label="p_signif",label.size=5,hide.ns=T)+
  # 手动设置形状的映射
  scale_shape_manual(values = c(21,22,23,24)) +
  # 手动设置填充色的映射
  scale_fill_manual(values=c("#679289","#ee2e31","#c9cba3","#f4c095"))+
  # 手动设置颜色的映射
  scale_color_manual(values=c("#679289","#ee2e31","#c9cba3","#f4c095"))+
  # 设置y轴的范围和标签
  scale_y_continuous(guide = "prism_minor",
                     limits = c(0, 40),
                     expand = c(0, 0))+
  labs(x=NULL,y="CAR T cells x (10<sup>3</sup>/g)")+
  theme_prism()+   # 使用prism主题
  # 自定义主题设置
  theme(strip.background = element_blank(),
        legend.background = element_rect(color=NA),
        legend.key = element_blank(),
        legend.spacing.x = unit(-0.09,"in"),
        legend.spacing.y = unit(-0.09,"in"),
        legend.text = element_text(color="black",size=6,face="bold"),
        legend.position = "top",
        axis.text.x=element_blank(),
        axis.text.y=element_text(color="black",size=8),
        axis.title.y = element_markdown(color="black",face="bold",size=10),
        axis.ticks.x = element_blank())+
  # 自定义图例设置
  guides(shape = guide_legend(override.aes = list(size=3),
                              direction = "horizontal",
                              nrow=3, byrow=TRUE))

```


![](https://files.mdnice.com/user/7338/4eac9a04-4782-482e-9fe1-78b7620d30a7.png)

>本节内容介绍到此结束，过程仅供参考；有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员文档**同步更新中**售价149元**，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询。

### 淘宝扫一扫

![](https://files.mdnice.com/user/7338/fc6e7ace-03ff-4d5b-b945-8aac5ab9212a.png)


### 2023会员群案例展示
![](https://files.mdnice.com/user/7338/beb078bb-c959-434f-8990-ed65c067a2fc.png)
![1](https://files.mdnice.com/user/7338/5c8d56e1-ba77-45f8-899d-5176b7231d2e.png)
![2](https://files.mdnice.com/user/7338/392ddaf1-ac11-4477-86c5-32e3ddc88823.png)
![3](https://files.mdnice.com/user/7338/0ac4cdcd-17d1-4594-84c6-adcbafeb60cf.png)
![4](https://files.mdnice.com/user/7338/ac5cb532-2881-44ca-82a8-e6aaedba73cd.png)
![5](https://files.mdnice.com/user/7338/58a277f7-1c3b-4972-a815-3d0235af45dc.png)
![6](https://files.mdnice.com/user/7338/bc4e1dfc-c800-4cb8-8943-6b2762f169ca.png)
![](https://files.mdnice.com/user/7338/a3de6a29-60b7-4d50-9bf1-40d563dd529f.png)
![](https://files.mdnice.com/user/7338/6834cd53-af20-4eb1-ab0c-031ad6e44469.png)



