>本节来复现**imeta**上的一张图`相关性组合热图`该图相关的表现形式小编以前有做过介绍，但是此图也有些许的不同，`此篇论文中的绝大多数图表小编以前都有做过相关案例介绍，可见认真学习以往的绘图安排达到论文发表的水平绰绰有余`。**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载** 

### 论文原图
Destabilized microbial networks with distinct performances of abundant and rare biospheres in maintaining networks under increasing salinity stress


![](https://files.mdnice.com/user/7338/1d54d147-4ea7-47ba-8f55-0fb6a3dcbb58.png)

可以看到Figure-1均为基础图表，整张图色调一致富有美感，拼图也是经过了一番讲究；从包括地图在内所有的图表以往都有案例介绍，感兴趣的朋友可以去翻阅以往的案例。

### 淘宝店铺个性化绘图服务
>有需要找小编做个性化绘图的朋友欢关注**淘宝店铺(R语言数据分析指南)** 有需求者可咨询下单。

### 复现图

![](https://files.mdnice.com/user/7338/eda799f9-2b52-47a1-b6a0-3448c287faee.png)

>此图的说其难度倒也没有，主要还是在于各个细节的调整如何设置几何对象的映射以及拼图参数的调整。下面来看具体绘图代码。
### 加载R包
```r
library(tidyverse)
library(readxl)
library(linkET)
library(RColorBrewer)
library(ggtext)
library(magrittr)
library(ggnewscale)
library(cowplot)
```
### 数据格式转换
因为存在特殊字符因此在绘图初始将其转换为**markdown**格式。
```r
varechem <- read_excel("imt279-sup-0002-supplementary_tables_1.xlsx",skip = 1) %>% select(pH:DOC) %>% 
  dplyr::rename("NO<sub>3</sub>-"=`NO3-N`,
                "NO<sub>2</sub>-"=`NO2-N`,
                "NH<sub>4</sub>+"=`NH4-N`,
                "PO<sub>4</sub>3-"="PO4")
  
varespec <- read_excel("imt279-sup-0002-supplementary_tables_1.xlsx",skip = 1,sheet = 2) %>% select(-1)
```
### 相关性分析
```
mantel <- mantel_test(varespec,varechem,
                      spec_select = list(" "= 1:9)) %>% 
  mutate(rd = cut(r, breaks = c(-Inf, 0.2, 0.4, Inf),
                  labels = c("< 0.2", "0.2 - 0.4", ">= 0.4")),
         pd = cut(p, breaks = c(-Inf, 0.01, 0.05, Inf),
                  labels = c("0.001-0.01","0.01-0.05", ">= 0.05")))
```
### 绘制下方条形图
```r
df <- mantel %>% arrange(desc(r))
df$env <- factor(df$env,levels = df$env)
p2 <- df %>% ggplot(aes(env,r,fill=pd))+
  geom_col(width=0.8)+
  scale_fill_manual(values =c("#788CAE","#EA967B","#FBD4AA")) +
  scale_y_continuous(expand = c(0,0))+
  theme_classic()+
  labs(x=NULL,y=NULL)+
  theme(axis.text.x = element_markdown(color="black",angle = 40,vjust =1,hjust = 1),
        axis.text.y=element_text(color="black"),
        panel.background = element_blank(),
        plot.background = element_blank(),
        legend.text = element_text(color="black"),
        legend.background = element_blank(),
        legend.title = element_blank(),
        legend.key = element_blank(),
        legend.position ="bottom",
        legend.key.height = unit(0.4,"cm"),
        legend.spacing.y = unit(0.4,"cm"))
```
### 绘制热图
```r
p1 <- qcorrplot(correlate(varechem,method = "pearson"),diag=T,type="upper")+
  geom_point(aes(size=abs(r)),shape=21)+
  scale_fill_gradientn(colours = RColorBrewer::brewer.pal(8,"RdYlBu"))+
  scale_color_gradientn(colours = RColorBrewer::brewer.pal(8,"RdYlBu"))+
  guides(size="none")+  # 移除size图例
  new_scale("size")+  # 因为要对size做两次映射，因此在此更新
  geom_couple(aes(colour=pd,size=rd),data=mantel,label.colour = "black",
              curvature=nice_curvature(0.1,by="from"),nudge_x=-1,
              offset_x=3,
              label.fontface=0,
              label.size =4,drop = T)+
  scale_size_manual(values = c(0.5, 1, 2)) +
  scale_colour_manual(values =c("#788CAE","#EA967B","#FBD4AA")) +
  guides(size = guide_legend(title = "Mantel's r",override.aes = list(colour = "grey35"), order = 2),
         color="none",
         fill = guide_colorbar(title = "pearson's r",order = 3))+
  theme(plot.margin = unit(c(-1.8,-2,1,-2),units="cm"),
        panel.background = element_blank(),
        axis.text=element_markdown(color="black",size=10),
        legend.key = element_blank(),
        legend.background = element_blank()) 
```
### 拼图
```r
# 拼图参数要根据图形大小多次调整从而找到最佳值
ggdraw()+draw_plot(p1,scale=0.63,y=0.045,width = 1,height = 1)+
  draw_plot(p2,scale=0.6,x=-0.2,y=-0.15,width=1.2,height=0.7)
# 保存图形
ggsave(plot,file="heatmap.pdf",width=4.7,height =4.98,unit="in",dpi=300)
```



